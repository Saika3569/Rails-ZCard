<h1><%= @board.title %></h1>
<%= link_to "新增文章", new_board_post_path(@board) %>

<%# <%=p @posts.to_a.length %> %>

<%# 上面問提 ActiveRecord_AssociationRelation 是什麼 %>

<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post %></li>
  <% end %>
</ul>